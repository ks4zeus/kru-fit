{
  "name": "kru-fit-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --persist-to ./wrangler-state",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.202",
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
